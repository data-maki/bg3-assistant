# BG3 Honor Mode Assistant Completion Audit

Evidence snapshot: 2026-07-13. `Proved` means the current worktree has direct test or runtime evidence. `Pending live check` is not counted as complete.

| Requirement | Status | Authoritative evidence |
|---|---|---|
| Detect and launch BG3 | Proved | Packaged app reports Larian process `bg3`; identity regression rejects the assistant's own bundle; Steam ID 1086940 is wired. |
| Capture the real BG3 window | Proved | Live enumeration exposed a 2560×44 auxiliary Larian strip plus the 2560×1440 Metal game surface. Production capture now selects the largest matching window and captures it 1:1; the retained post-fix frame is 2560×1440. |
| Twilight Cleric pet above BG3 | Proved | The embedded reference atlas passes the hatch-pet v2 validator at 1536×2288 with 11 rows, no errors, and no warnings. Rest is deterministically row 0/frame 0 with its timeline paused; hover plays the authored jump timing, then maps pointer angle clockwise across all 16 look cells. Native regressions cover rest, jump timing, deadzone, and four cardinals; the packaged control exposes `Resting`/`Awake` accessibility state. The overlay remains clamped outside the minimap/hotbar bands. |
| Single overlay ownership | Proved | A per-user nonblocking OS lock is acquired before app launch. A second launch activates the owner and exits; launching the repository app and extracted signed ZIP together retained one GUI process, one embedded backend, and one port-8787 listener. The owner also retires legacy assistant copies already running at startup. |
| Pet dragging and safe placement | Proved in implementation; automated drag unavailable | `isMovableByWindowBackground`, explicit drag handles, persisted normalized anchor, right-edge/vertical-center resize preservation, and visible-screen clamping are active. The default is true middle-right; anchor schema v7 migrates prior portrait-card and resize-contaminated values once. Only real `leftMouseDragged` events persist placement. The packaged Details transition retained `0.9884,0.5`. Desktop automation can inspect and click but not drag the borderless system-dialog panel. |
| Compact current objective, destination, level, danger | Proved | Final extracted package exposes only activity/level/danger, objective, one `AVOID` instruction, and separately accessible Details/Ask/Done controls. |
| Planner and compact manual progression | Proved | Compact pet exposes `Done`; live use produced the shared unchecked/irreversible override and was cancelled to preserve player state. Native and backend regressions prove completed Nautiloid advances to Grove Entrance. Planner also exposes complete/skip/revisit and optional skip notes. |
| Combat-sized pinned card | Proved | Live `Ready & pin` collapsed to a PINNED Commander Zhalk card with danger, key advice, and Unpin. |
| All 19 checkpoints and reviewed route | Proved | `/api/act1/route` returns 19 ordered checkpoints; backend tests assert order, preparation, failures, completion, level, region, and coordinates. |
| Concrete next objective or blocker | Proved | First pending checkpoint is deterministic; unavailable guide and Act 2 states explain blockers rather than claiming completion. |
| Local per-run persistence | Proved | Native smoke test round-trips state atomically and verifies per-run archive creation; active run plus run-ID snapshots are documented. |
| Guide-version pinning | Proved in model/code | First load pins the version; later versions show a notice and do not overwrite an active run. New runs adopt the available version. |
| Party composition and build-aware readiness | Proved | Final packaged Party tab shows one custom character and three story-companion selectors, one party-wide act selector, quick party/individual level controls, and a reviewed-build picker per slot. Shadowheart, Lae'zel, Astarion, Gale, Wyll, and Karlach are built in. Each slot shows only the current level choice plus act-relevant equipment and locations; approved builds are assumed setup. Backend tests verify capability and build assignments change guidance. |
| BG3-aligned adaptive Liquid Glass | Proved | On macOS 26.5.1, the packaged collapsed state is a 304×128 middle-right tooltip; Current is 420×500 and Party is 420×590 with all four assigned builds visible together. Native glass now carries translucent umber, bronze/gold double framing, parchment/serif hierarchy, gold navigation, and restrained lethal-state tinting. macOS 14–15 retain a system-material fallback. No BG3 artwork is redistributed. |
| Eight detailed, source-attributed builds | Proved | API returns eight plans, 90 level rows, original sources, tactics, caveats, and Act 1 gear; live Flamadin selection showed current/next level and source before being restored. |
| Act 2 readiness gate | Proved | Gate remains visible across planner tabs; skipped or pending major/irreversible checkpoints remain blockers. |
| Grounded contextual chat | Proved | Live quick Ask returned labeled Guide and Assistant sections. Tests cover readiness, build-level choice, screenshot-as-suggestion, and unknown boundaries. |
| No automatic vision completion | Proved | Vision only returns candidates; player selection changes context, and explicit confirmation is required for completion. |
| Two-second local detector; no continuous cloud vision | Proved | ScreenCaptureKit loop calls local alignment/OCR only; cloud analysis is button-triggered; debug screenshot storage is opt-in. macOS preflight authorization enables automatic verification without a manual button, and BG3 discovery includes fullscreen windows in other Spaces. |
| Wilderness pan/zoom/resize marker transform | Positive live registration proved; packaged overlay/pan/zoom pending | A real loaded-save Wilderness frame registered with 319 inliers at confidence 1.0, and the corrected 2560×1440 frame registered with 132 inliers at confidence 1.0 with four on-screen fight projections. The final packaged loop is capture-active and correctly rejects the current gameplay screen, but still needs observation on a reopened map followed by one pan, zoom, and window-geometry change. |
| Interior-region markers | Proved as calibrated fallback | Non-Wilderness regions bypass Wilderness artwork matching and use region-specific bounds plus per-region persisted zoom/pan calibration. Live interior-map alignment remains manual by design. |
| Localhost Act 1 map | Proved | `/map` exposes 56 markers (19 fights, 37 item pins), eight build filters, level/readiness filters, MapGenie attribution/handoff, live position API, and no observed browser console errors. |
| Per-member web party and equipment | Proved | Native handoff and shared state preserve four member IDs, names, individual levels, and builds. Party renders only each member's current-level action; Equipment is a separate Act 1 tab grouped and persisted per member. Mixed-level and assignment/reload browser QA passed, and API tests no longer mutate developer/player state. |
| One-shot named marker export | Export/alignment proved; live BG3 placement pending | The map exports only safe unresolved fights in the current travel phase plus unequipped items for selected builds, with guide-vs-suggestion level authority and unique ≤36-character labels. Preview activates the exact queue in the native alignment overlay; JSON export, persisted fingerprint confirmation, duplicate suppression, and overlay clearing pass browser/API tests. The external Computer Use click/name sequence still requires a real open BG3 map. |
| Self-contained release package | Proved locally; public signing pending | The 177 MB arm64 app embeds the frozen backend and launches from `/tmp` without the source checkout, Python, or `uv`. Its APIs returned 19 checkpoints, eight builds, 56 markers, and the map; process ancestry proved the backend was the app's child, and Quit stopped it and released port 8787. The 81 MB ZIP and its extracted app pass strict code-sign verification. |
| Privacy and non-goals | Proved by audit | No game-memory reading, mods, save manipulation, combat automation, native input control, or continuous LLM screenshot loop. The only external input scope is an explicit Computer Use session inside BG3's open custom-marker flow. |
| Tests, signing, and documentation | Proved for local release | 43 backend tests, native model/detector/map/persistence/capture-selection/overlay-geometry checks, Swift release build, strict code-sign verification of the app and extracted ZIP, plist validation, README, release checklist, and LABREPORT. The available Apple Development and Apple Distribution identities are not a public Developer ID/notarized signature. |

## Remaining completion evidence

1. Launch BG3 in windowed/borderless mode, load a save, and leave the Wilderness map visible.
2. Export the current marker queue and observe its exact labels in the click-through overlay.
3. Use the external Computer Use session to create and name the custom markers; stop on any non-map UI.
4. Pan once, zoom once, and change window geometry once; verify markers/overlay remain aligned, then confirm the fingerprint as placed.

Until those three observations are recorded, the full goal remains active.

Public distribution additionally requires publisher-owned release inputs that are not present in the repository: a permanent bundle ID, Developer ID Application certificate, and notarization keychain profile. `mac/scripts/build-release.sh` now fails closed on those requirements when `REQUIRE_RELEASE_SIGNING=1`.
